<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <h4><%= pluralize(object.errors.count, t(".error")) %><%= t(".error_message") %></h4>
    </div>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
